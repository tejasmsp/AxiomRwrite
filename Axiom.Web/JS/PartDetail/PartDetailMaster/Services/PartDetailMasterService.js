app.service('PartDetailMasterService', function ($http, configurationService) {
    var PartDetailMasterService = [];

    //PartDetailMasterService.GetProposalFees = function (OrderId, PartNo) {
    //    return $http.get(configurationService.basePath + "GetProposalFees?OrderId=" + OrderId + "&PartNo=" + PartNo);
    //};
    

    return PartDetailMasterService;
});